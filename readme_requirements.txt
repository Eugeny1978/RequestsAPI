// На сервере, с которого осуществляется перенос, выполняем следующую команду указывая путь к файлу,
// в котором будет сохранена информация которая потребуется для корректной работы проекта
pip freeze  > requirements.txt


// Установка с помощью этого файла происходит:
pip install -r requirements.txt

//Библиотеки в данном случае:
requests
beautifulsoup4
lxml
XlsxWriter

// Что должно получиться см файл